{
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.0.0"
  },
  "scripts": {
    "deploy": "sls deploy",
    "invoke": "sls invoke local --function certificate --path data.json",
    "remove": "sls remove"
  }
}
